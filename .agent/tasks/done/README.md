# Completed Tasks

> Archive of finished work. Each file is named `YYYY-MM-DD-<slug>.md`.
> Used by memory agents to extract patterns and lessons learned.

Move completed tasks here from `current.md` with the completion date.
