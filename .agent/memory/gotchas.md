# Gotchas

> Things that bit us and will bite again if forgotten. Short, grep-friendly entries.
> Updated by the `/update-memory` prompt after debugging sessions.

---

<!--
- **[Category]** <Problem description> → <Fix or workaround> (YYYY-MM-DD)

Examples:
- **[Terraform]** `azurerm_storage_account` names can't have hyphens → use `st` prefix, no separators (2026-03-15)
- **[Ansible]** `cloud-init status --wait` returns rc=2 on warnings → accept `[0, 2]` (2026-02-20)
- **[Rust]** `HashMap` iteration order is non-deterministic → use `BTreeMap` for reproducible state (2026-01-10)
-->
